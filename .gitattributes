# Normalize text line endings in the repo; check out native per-OS.
* text=auto

# Preserve sample files byte-for-byte (BUFR is binary; the .buf fixtures are
# compared byte-by-byte against generated output, so git must not touch them).
*.buf binary
samples/rrfs/bufr.* binary
