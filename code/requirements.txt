# RRFS -> BUFKIT pipeline - Python dependencies
# Tested with Python 3.11 (Anaconda).  Versions below are known-good.

# --- core pipeline (tools/update_rrfs.py, bufkit_replica/{rrfs_bufr,parser,writer}.py) ---
numpy==1.26.4
pandas==2.2.2
MetPy==1.6.1            # convective indices + thermodynamics validation
pybufrkit==0.2.25      # pure-Python BUFR decoder (data messages)
boto3==1.34.95         # AWS S3 access to noaa-rrfs-pds (no credentials needed)
huggingface_hub==1.14.0  # publishes finished .buf files to the HF dataset

# eccodes (ECMWF) - used to read the embedded NCEP BUFR tables. The Python
# package needs the ecCodes C library; install BOTH via conda (recommended):
#     conda install -c conda-forge eccodes python-eccodes
eccodes

# --- optional: only for the GRIB2 acquisition path; NOT needed for the RRFS
#     BUFR pipeline that feeds real BUFKIT ---
# herbie-data         # GRIB2 acquisition
# xarray              # GRIB2 field handling
